{
  "$schema": "https://www.raycast.com/schemas/extension.json",
  "name": "discord-quick-switcher",
  "title": "Discord Quick Switcher",
  "description": "Open Discord channels, DMs, and threads from a pre-generated channels.json file.",
  "icon": "command-icon.png",
  "author": "suhas",
  "license": "MIT",
  "commands": [
    {
      "name": "open-discord-channel",
      "title": "Open Discord Channel",
      "description": "Search and open Discord deep links",
      "mode": "view"
    }
  ],
  "preferences": [
    {
      "name": "channelsPath",
      "title": "Channels JSON Path",
      "description": "Path to the generated channels.json file.",
      "type": "textfield",
      "required": false,
      "default": "~/channels.json"
    }
  ],
  "scripts": {
    "dev": "ray develop",
    "build": "ray build -e dist",
    "lint": "ray lint",
    "test": "node --test tests/*.test.mjs"
  },
  "dependencies": {
    "@raycast/api": "^1.104.23",
    "raycast": "^1.0.8"
  },
  "devDependencies": {
    "typescript": "^7.0.2"
  }
}